// main() -> Função principal, é a parte onde após a compilação que toda execução do programa se inicia.
// stdio.h -> Biblioteca de entrada e saída de dados (input output) padrão para todo projeto C.
// printf() -> Função que printa uma informação dentro do terminal/console do sistema.

#include <stdio.h>

int main() {
    printf("Hello world!\n");

    return 0;
}